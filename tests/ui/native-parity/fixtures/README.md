# Native Parity UI Fixtures

- `parity-fixture-solution.synthetic.json` is a **SYNTHETIC** fixture manifest used by UI parity tests.
- It targets workspace `default` and layout candidates centered on `Asset Details`.
- Replace or augment with real DDR-derived fixture manifests for stronger production parity validation.
