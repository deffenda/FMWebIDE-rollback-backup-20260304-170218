# DDR Fixtures

This directory contains fixtures for parity and layout-fidelity harnesses.

- `synthetic-minimal-ddr.xml` is a **synthetic** fixture used when no real DDR samples are bundled in-repo.
- Replace or add real DDR fixtures for stronger parity validation.
